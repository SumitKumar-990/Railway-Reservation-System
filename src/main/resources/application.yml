spring:
  application:
    name: railway-reservation-system
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
  jackson:
    serialization:
      write-dates-as-timestamps: false

app:
  jwt:
    secret: ${JWT_SECRET:a-very-long-secret-key-for-jwt-signing-minimum-256-bits-long-change-in-production}
    expiration-ms: 86400000
  booking:
    hold-minutes: 15
  railradar:
    api-key: ${RAILRADAR_API_KEY:}
    base-url: https://api.railradar.in/v1

server:
  port: 8080
